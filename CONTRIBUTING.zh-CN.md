# 参与 Rear 开发

[English](CONTRIBUTING.md)

## 分支与评审

沿用 Gear 的主题分支方式：从最新的 `origin/dev` 创建分支，以功能命名，
例如 `codex/trace-analysis-workbench`；日常 PR 的目标分支为 `dev`。
一个分支聚焦一个功能或修复。提交标题使用 `feat: ...`、`fix: ...`、
`docs: ...` 等形式。推送前检查完整 diff，清理临时调试代码和本地实验数据。

评审说明应包含具体问题、修改后的行为、兼容性影响和验证结果。
开发分支推送不会发布 npm 包；版本更新与发布需单独安排。

## 开发与验证

使用 Node.js 22.x 中的 22.19 及以上版本，或 24 及以上版本，以及 npm：

```sh
npm ci
npm run typecheck
npm run build
npm test
npm pack --dry-run --ignore-scripts
```

全量测试前先构建：打包测试会读取 `lib/client.js`。只检查源码时可运行
`npm test -- tests/plugin/trace-chat.spec.ts`。`npm run pack:check` 只构建一次，
然后核对打包内容。CI 在 `dev`/`main` 推送、目标为这两个分支的 PR 和手动运行时
执行相同检查。自动化测试不需要模型密钥，也不依赖正在运行的 Gear/Hitch 服务。

保留证据归属、持久化、重试、取消及宿主集成的回归测试。复用小型合成样例和
临时目录，避免重复验证实现细节，不为纯文案或样式修改堆积测试。
涉及原生 Chat、语言切换和布局的改动，还需实际检查浏览器：空白会话与历史恢复、
中英文、宽窄布局、模型和权限控件、导航及控制台错误。注明是否进行了真实模型调用；
准备空白会话本身不产生推理。

## 代码、证据与文档

遵循相邻 TypeScript 代码的风格：两个空格、单引号、不加分号。
Rear 使用 `.ts`/`.tsx` 源码导入，由 TypeScript 在编译时重写扩展名。
运行时校验、RPC 类型和调用方必须一致。DSH 私有接口兼容逻辑集中在适配器中，
按文档注明的宿主版本验证。修改依赖时同步 `package-lock.json`。

Gear 管优化，Hitch 管执行证据，Rear 读取它们的状态。保留实验与运行的准确归属、
校验和与来源偏移；分析快照、会话历史和原始证据分别保存。不静默截断导出、不改写
历史结果、不重放提交结果不确定的首条消息，也不在恢复时覆盖用户的模型或权限设置。

界面词典和两份 README 同步更新。翻译界面标签，保留归档消息和协议标识的原文。
区分当前使用说明、设计提案和本地调试笔记。

不要提交 `lib/`、`outputs/`、`trace-chats/`、生成的仪表盘数据、密钥、个人路径或
会话日志。检查 npm 包只包含运行代码、类型声明、加载补丁、两份 README、许可证和第三方声明；
测试源码和本地分析工作区不随包分发。
